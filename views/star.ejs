<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Last</title>

  <!-- Font Awesome -->
  <script src="https://kit.fontawesome.com/26192e937e.js" crossorigin="anonymous"></script>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</head>

<body>
  <div class="container px-4" style="margin-top: 80px; max-width: 600px;">
    <nav class="navbar bg-light fixed-top m-auto" style="max-width: 600px;">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">🀄 All Last</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="offcanvas offcanvas-end offc" style="width: 200px;" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
          <div class="offcanvas-header">
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
          </div>
          <div class="offcanvas-body">
            <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">홈</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/signup">회원가입</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/signin">로그인</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/record">기록하기</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/log">마작 로그</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/score">나의 점수</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">로그아웃</a>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </nav>
    <h3>즐겨찾기</h3>

    <% if(games.length > 0) { %>
    <% for(let i = 0; i < games.length; i++) { %>
    <table class="table caption-top table-borderless text-center">
      <caption class="">
        <span><%= games[i].date %>, <%= games[i].place %></span>
        <a class="ms-1" data-bs-toggle="modal" data-bs-target="#starModal" data-bs-starID=<%= games[i].star_id %>><i class="fa-regular fa-pen-to-square"></i></a>
        <a class="ms-1" onclick="deleteStar('<%= games[i].star_id %>')"><i class="fa-solid fa-trash"></i></a>
      </caption>
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">작사</th>
          <th scope="col">점수</th>
          <th scope="col">우마</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th scope="row">1</th>
          <td><%= games[i].player_1 %></td>
          <td><%= games[i].score_1 %></td>
          <td><%= total[i].total_1 %></td>
        </tr>
        <tr>
          <th scope="row">2</th>
          <td><%= games[i].player_2 %></td>
          <td><%= games[i].score_2 %></td>
          <td><%= total[i].total_2 %></td>
        </tr>
        <tr>
          <th scope="row">3</th>
          <td><%= games[i].player_3 %></td>
          <td><%= games[i].score_3 %></td>
          <td><%= total[i].total_3 %></td>
        </tr>
        <tr>
          <th scope="row">4</th>
          <td><%= games[i].player_4 %></td>
          <td><%= games[i].score_4 %></td>
          <td><%= total[i].total_4 %></td>
        </tr>
        <tr>
          <td colspan="4" class="text-start memo"><%= games[i].memo %></td>
        </tr>
      </tbody>
    </table>
    <% } %>
    <% } %>

    <div class="modal fade" id="starModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5">즐겨찾기</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body">
            <form action="/log/star/edit" method="POST">
              <input type="text" class="form-control visually-hidden" name="starID">
              <div class="mb-3">
                <label for="memo-text" class="col-form-label">메모:</label>
                <textarea class="form-control" id="memo-text" name="memo" rows="3"></textarea>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                <button type="submit" class="btn btn-primary">저장</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

  </div>
</body>

<script>
  const modalEl = document.getElementById('starModal');
  modalEl.addEventListener('show.bs.modal', event => {
    const button = event.relatedTarget;
    const id = button.getAttribute('data-bs-starID');
    const memoValue = button.parentNode.parentNode.querySelector('.memo').textContent;

    const modalInput = modalEl.querySelector('.modal-body input');
    const modalTextarea = modalEl.querySelector('.modal-body textarea');

    modalInput.value = id;
    modalTextarea.value = memoValue;
  })

  function deleteStar(id) {
    fetch(`/log/star/${Number(id)}/delete`, {
      method: 'delete',
      headers: {
        'Content-type': 'application/json'
      },
    }).then((res) => {
      location.href = '/log/star';
    })
  }
</script>

</html>